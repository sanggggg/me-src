---
interface Props {
  class?: string;
  href: string;
  [key: string]: unknown;
}

const { class: className, href, ...attributes } = Astro.props;
---

<a class={className} href={href} {...attributes}>
  <slot />
</a>
